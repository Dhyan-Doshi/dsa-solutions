class Solution {
public:
    int lengthOfLastWord(string s) {
        int count=0;
        reverse(s.begin(),s.end());
        for(int i=0;i<s.length();i++){
            if(s[i]!=' '){
                count++;
            }else if(s[i]==' ' && count>0){//Very Very important as isse vo case handel ho jayege jaha starting space se
                break;
            }
        }
        return count;
    }
};